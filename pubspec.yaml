name: data_monitor_generators
description: "A package that encapsulates Build Generators for the data_monitor package."
repository: https://github.com/LozCool/data_monitor_generators
version: 1.0.10

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  analyzer: ^13.0.0
  build: ^4.0.6
  source_gen: ^4.2.3

  data_monitor: ^1.0.10

dev_dependencies:
  build_runner: ^2.15.0
  flutter_lints: ^6.0.0

topics:
  - codegen
  - state-management
